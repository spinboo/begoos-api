const makeUserModel ({ database }) => {
    return Object.freeze({
        add,
        findByEmail,
        findById,
        get,
        remove,
        replace,
        update
    });

    const add
}

export default makeUserModel;